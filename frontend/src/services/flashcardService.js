export function getFlashcards(chapterId) {
  void chapterId
  // TODO: GET /chapters/:chapterId/flashcards
  return []
}

export function updateFlashcardProgress(flashcardId, progress) {
  void flashcardId
  void progress
  // TODO: PUT /flashcards/:flashcardId/progress
  return null
}
